#include "PlayerMovement.h"
#include "Window.h"



void PlayerMovement::ClipBorder()
{
	/*
	if (m_transform.GetPosition().x >= (window.GetWidth() - 45))
		m_transform.GetPosition().x = window.GetWidth() - 45;

	if (m_transform.GetPosition().x <= (45))
		m_transform.GetPosition().x = 45;

	if (m_transform.GetPosition().y >= (window.GetHeight() - 45))
		m_transform.GetPosition().y = window.GetHeight() - 45;

	if (m_transform.GetPosition().y <= (45))
		m_transform.GetPosition().y = 45;
		*/
		}